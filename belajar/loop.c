#include <stdio.h>

int main (){
    // for loop, kata pa riza enih kek penjelmaan dari while loop
    for(int i = 69; i >= 0; i-=3)
    {
        printf("%d\n", i);
    }

    // while loop

    // repeat until, kalo di c lang pake do while

    // iterate stop, c lang ga mendukung kata pa riza

    // counter transversal

    // repeat n times, c lang ga mendukung kata pa riza

    return 0;
}